<?xml version="1.0" encoding="utf-8"?>
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Slab Passes - Filtro Avançado"
        Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,15">
            Filtro Avançado por Parâmetro
        </TextBlock>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,15" Foreground="#666">
            Escolha um parâmetro para AGRUPAR os tubos além do diâmetro.
            Exemplo: Se escolher "Stage", tubos de 2" com "Riser" e "Vent" serão agrupados separadamente.
        </TextBlock>

        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="params_panel"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btn_skip" Content="Pular (Apenas Diâmetro)" Width="150" Height="32" Margin="0,0,10,0"/>
            <Button x:Name="btn_continue" Content="Continuar com Filtro" Width="150" Height="32"
                    Background="#1976D2" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>